<Window x:Class="ForumProj.Windows.UserQuestionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:ForumProj.Windows"
        mc:Ignorable="d"
        Title="UserQuestionWindow" Height="450" Width="800">
    <ScrollViewer>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock x:Name="InfoBlock" Grid.Row="0" 
                       Text="Your QnA"
                       Foreground="#2E3440"
                       FontSize="24"
                       HorizontalAlignment="Center"
                       FontStyle="Italic"
                       Margin="10" />
            <StackPanel Grid.Row="1" x:Name="UserQaStockPanel" 
                        Orientation="Vertical" 
                        Background="Transparent"
                        Margin="50,20">
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
